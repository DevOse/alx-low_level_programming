#include "main.h"
/**
 * rev_string - prints a string, in reverse, followed by a new line
 * @s: char
 * Return: 0
 */

void rev_string(char *s)
{
	int a = 0;
	int b = a - 1;
	int c;

	while (s[a] != 0)
	{
		_putchar a++
	}
	for (; b >= 0; b--){
		for
		_putchar(s[b]);

}
